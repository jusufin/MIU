



var MainWindow = Ti.UI.createWindow({
	
	backgroundColor:"#1B5F65"

}); //creates window and sets backround color

var MainView = Ti.UI.createView({
		backgroundColor: "#A6782B",
		borderRadius: 5,
		borderWidth: 2,
		width: 300,
		height: 450,
		top: 30
});


MainWindow.add(MainView);
MainWindow.open();
